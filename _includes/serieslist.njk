{% set currentSeries = data.series %}
<h3>Related Posts in {{ currentSeries }} Series</h3>
<ul>
    {% for post in collections.posts | postsWithMetadata("series", currentSeries)%}
        <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
    {% endfor %}
</ul>